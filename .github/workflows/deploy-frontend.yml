name: Deploy to VPS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Compress codebase
      run: |
        tar --warning=no-file-changed -czf frontend.tar.gz \
          --exclude='./.git' \
          --exclude='./node_modules' \
          --exclude='./.next' \
          --exclude='frontend.tar.gz' . || true

    - name: Transfer compressed codebase to VPS
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        source: "frontend.tar.gz"
        target: "/tmp/frontend.tar.gz"

    - name: Deploy on VPS
      uses: appleboy/ssh-action@v1
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        script: |
          sudo mkdir -p /var/www/fbafrontend
          sudo mv /tmp/frontend.tar.gz /var/www/fbafrontend/
          cd /var/www/fbafrontend
          sudo tar -xzf frontend.tar.gz
          sudo rm frontend.tar.gz

          sudo docker compose down || true
          sudo docker compose up --build -d
