version: "3.9"
services:
  fba-frontend:
    build:
      context: .
      target: runner
    container_name: fba-frontend
    ports:
      - "3000:3000"
    restart: unless-stopped
    env_file:
      - .env